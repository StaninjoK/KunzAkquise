const menuButton=document.querySelector('.menu-toggle');
const navigation=document.querySelector('.nav-links');
if(menuButton&&navigation){menuButton.addEventListener('click',()=>{const open=menuButton.getAttribute('aria-expanded')!=='true';menuButton.setAttribute('aria-expanded',String(open));navigation.classList.toggle('is-open',open);});navigation.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{menuButton.setAttribute('aria-expanded','false');navigation.classList.remove('is-open');}));document.addEventListener('keydown',e=>{if(e.key==='Escape'){menuButton.setAttribute('aria-expanded','false');navigation.classList.remove('is-open');}});}
const enquiry=document.querySelector('#enquiry');
if(enquiry){const requested=new URLSearchParams(location.search).get('leistung');const field=enquiry.elements.namedItem('leistung');if(requested&&[...field.options].some(o=>o.value===requested))field.value=requested;enquiry.addEventListener('submit',e=>{e.preventDefault();if(!enquiry.reportValidity())return;const d=new FormData(enquiry);const body=`Guten Tag Herr Kunz,\n\nich interessiere mich für ${d.get('leistung')}.\n\nName: ${d.get('name')}\nUnternehmen: ${d.get('firma')}\nE-Mail: ${d.get('email')}\n\nMein Vorhaben:\n${d.get('nachricht')}\n\nBitte kontaktieren Sie mich zu meiner Anfrage.`;const subject='Projektanfrage '+d.get('leistung')+' – '+d.get('firma');location.href='mailto:stanley.kunz@kunzakquise.com?subject='+encodeURIComponent(subject)+'&body='+encodeURIComponent(body);document.querySelector('#form-feedback').textContent='Der E-Mail-Entwurf wird in Ihrem E-Mail-Programm geöffnet. Bitte senden Sie ihn dort ab. Falls sich kein Programm öffnet, schreiben Sie uns direkt oder nutzen Sie WhatsApp.';});}
